spring:
  thymeleaf:
    cache: true

logging:
  level:
    root: INFO
    com.prismsearch: INFO
  file:
    name: ${LOG_FILE:/var/log/prismsearch/prismsearch.log}
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 14
      total-size-cap: 2GB

management:
  endpoint:
    health:
      show-details: never
