spring:
  thymeleaf:
    cache: false

logging:
  level:
    root: INFO
    com.prismsearch: DEBUG
    org.springframework.web: INFO
    reactor.netty.http.client: INFO

prismsearch:
  rate-limit:
    enabled: false
  cache:
    ttl-seconds: 60
    empty-ttl-seconds: 30
